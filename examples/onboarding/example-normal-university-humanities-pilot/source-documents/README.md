# Source Documents

This public onboarding example intentionally contains no real source DOCX/PDF files.

The requirements are fictionalized from a private workflow shape so the public repository can test template onboarding without committing private real-institution files, extracted text, user content, or font binaries.
