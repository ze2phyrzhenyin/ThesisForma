# Hooks

Placeholder for project hooks.

Recommended future hooks:

- run `dotnet format` before commit;
- run `dotnet test ThesisDocx.slnx` before merge;
- run `scripts/generate-example-docx` after renderer changes;
- reject tests that only assert DOCX file existence.
